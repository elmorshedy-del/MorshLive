.DS_Store
Thumbs.db
*.log
.env
.env.local
.dev.vars
.dev.vars.*
.wrangler/
node_modules/
.auth/
storage/
reports/
logs/

# Build output — regenerated by `npm run refresh:matches` on every deploy.
# `_redirects` points at these paths; they are never committed.
generated/
sitemap-schedule.xml
